<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dkha.test.dao.StudentDao">

    <resultMap type="com.dkha.test.entity.Student" id="studentMap">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="no" column="no"/>
        <result property="status" column="status"/>

        <collection property="books" ofType="com.dkha.test.entity.Book" column="s_id">
            <result property="id" column="b_id"/>
            <result property="name" column="b_name"/>
        </collection>

    </resultMap>

    <select id="selectByName" resultMap="studentMap">
        select * ,book.`name` as b_name from student,s_book, book where student.`id`=s_book.`s_id` AND s_book.`b_id`=book.`id` AND student.name = #{name};
    </select>

</mapper>
